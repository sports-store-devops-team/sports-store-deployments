apiVersion: v2
name: sports-store
description: Helm chart for the Sports Store microservices application
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: mongodb
    version: 19.1.22
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: mongodb.enabled
