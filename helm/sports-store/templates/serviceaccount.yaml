{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sports-store.serviceAccountName" . }}
  labels:
    {{- include "sports-store.labels" . | nindent 4 }}
{{- end }}
