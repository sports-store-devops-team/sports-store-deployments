# Documentation-only key contract. This is deliberately not a Kubernetes
# Secret and contains no credential or encoded value.
secretName: monitoring-grafana-admin
requiredKeys:
  - admin-user
  - admin-password
