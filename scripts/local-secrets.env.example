# Copy this file to local-secrets.env and keep that file outside Git.
# Generate URL-safe development values with commands such as:
#   openssl rand -hex 12   # MongoDB password
#   openssl rand -hex 32   # JWT secret

MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=
JWT_SECRET=
