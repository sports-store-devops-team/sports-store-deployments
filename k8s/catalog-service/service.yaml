apiVersion: v1
kind: Service

metadata:
  name: catalog-service
  namespace: sports-store
  labels:
    app: catalog-service

spec:
  selector:
    app: catalog-service

  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8002