apiVersion: v1
kind: Service

metadata:
  name: gateway
  namespace: sports-store
  labels:
    app: gateway

spec:
  type: NodePort

  selector:
    app: gateway

  # Local access: minikube service gateway -n sports-store
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
