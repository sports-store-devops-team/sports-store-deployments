apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: sports-store
  labels:
    app: payment-service

spec:
  selector:
    app: payment-service

  ports:
  - name: http
    protocol: TCP
    port: 8005
    targetPort: 8005
