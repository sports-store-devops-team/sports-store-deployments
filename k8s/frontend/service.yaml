apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sports-store
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
