apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: sports-store
  labels:
    app: auth-service

spec:
  selector:
    app: auth-service

  ports:
  - name: http
    protocol: TCP
    port: 8001
    targetPort: 8001
