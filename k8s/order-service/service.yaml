apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: sports-store
  labels:
    app: order-service

spec:
  selector:
    app: order-service

  ports:
  - name: http
    protocol: TCP
    port: 8004
    targetPort: 8004
